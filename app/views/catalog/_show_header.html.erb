<% doc_presenter = index_presenter(document) %>
<% # bookmark/folder functions   -%>
<% if document['author_name_tesim'].present?
    author_name = doc_presenter.field_value 'author_name_tesim'
    else
     author_name = ''
end %>
<%
  collection = t("view-specific.home.boxes.#{doc_presenter.field_value 'subcollection_ssi'}.title")
  description = author_name
  heading = doc_presenter.field_value 'work_title_tesim'
%>
<%= render :partial => 'shared/components/organisms/layout/1-column', locals: { elements:['page_intro_3'], classes:'white-cover-bg padding-top', heading: heading, description: description, collection:collection} %>

