<!DOCTYPE html>
<html lang="en" class="no-js">
<head>
  <meta charset="utf-8">
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
  <!-- No cache on pages, to prevent browser's 'back' button usage problem-->
  <!-- <meta name="turbolinks-cache-control" content="no-cache">  -->
  <!-- Mobile viewport optimization h5bp.com/ad -->
  <meta name="HandheldFriendly" content="True">
  <meta name="viewport" content="width=device-width,initial-scale=1.0">

  <!-- Internet Explorer use the highest version available -->
  <meta http-equiv="X-UA-Compatible" content="IE=edge">

  <!-- Mobile IE allows us to activate ClearType technology for smoothing fonts for easy reading -->
  <!--[if IEMobile]>
  <meta http-equiv="cleartype" content="on">
  <![endif]-->

  <title><%= render_page_title %></title>
  <%= opensearch_description_tag application_name, opensearch_catalog_url(format: 'xml') %>
  <%= favicon_link_tag %>
  <%= stylesheet_link_tag "application", media: "all" %>
  <%= javascript_include_tag "application_top" %>

  <%= csrf_meta_tags %>
  <%= content_for(:head) %>
</head>

<% if has_search_parameters?
     is_search_page = true
   else
     is_search_page = false
   end %>

<body class="<%= render_body_class %> theme-yellow <%= is_search_page ? 'menu-search-open' : '' %>">
<%= render partial: 'shared/header_navbar' %>
<%= render :partial => 'shared/components/atoms/main/index', locals: {content: 'shared/content', is_front:true} %>
<%= render partial: 'shared/footer' %>
<%= render partial: 'shared/modal' %>

<%= javascript_include_tag "application_bottom"  %>

</body>
</html>
