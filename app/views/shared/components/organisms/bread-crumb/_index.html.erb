<div class="org-01c45fc6-df1a-4fd8-b8c3-d1ff8e3b519d <%= classes %>">
  <div class="inner-content grid-container">
    <div class="grid-x grid-margin-x">
      <div class="cell small-12">
        <div class="bread-crumb">
          <%= render :partial =>'shared/components/atoms/icon/index', locals: {icon_type: 'home', icon_size: 'icon-xsmall'}%>
          <a href="http://www.kb.dk"><%= t('view-specific.breadcrumb.home')%></a>
<!--         / <a href="http://digitalesamlinger.kb.dk"><%#= t('view-specific.breadcrumb.digital_collections')%></a>-->
          <% items.each_with_index do | item, index |%>
          / <% if (index == items.size - 1) | (item[1] == '') %>
	          <% if (item[0].downcase == 'letters') %>
	          BREVE
                  <% else %>
                  <%= item[0]%>
                <% end %>		  
                <% else %>
                  <a href="<%= item[1] %>"><%= item[0]%></a>
                <% end %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
